Approach: just traverse the LL and check if the Node's data is eq to the search element!

class Solution {
    static boolean searchKey(int n, Node head, int key) {
        while(head!=null)
        {
            if(head.data ==key)
            {
                return true;
            }
            head=head.next;
        }
        return false;
    }
}

Tc: O(N)
sc: O(1)
